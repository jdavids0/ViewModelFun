@{
    ViewData["Title"] = "Multiple Users";
}

@model List<User>

<div class="container text-center">
    <h1 class="display-4">Here are some users!</h1>
    @{
        foreach(User user in Model)
        {
            <p>@user.FirstName @user.LastName</p>
        }
    }
</div>